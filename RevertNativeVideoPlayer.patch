--- Kino/UI/Viewer/ViewerView.swift
+++ Kino/UI/Viewer/ViewerView.swift
@@ -16,19 +16,17 @@
 
 // Wrapper untuk mematikan kontrol bawaan macOS agar tidak dobel
 struct NativeVideoPlayer: NSViewRepresentable {
     let player: AVPlayer
-    let transform: ClipTransform
     
-    init(player: AVPlayer, transform: ClipTransform) {
+    init(player: AVPlayer) {
         self.player = player
-        self.transform = transform
     }
     
-    func makeNSView(context: Context) -> PreviewPresentationLayer {
-        let layer = PreviewPresentationLayer()
-        layer.update(player: player, transform: transform)
-        return layer
+    func makeNSView(context: Context) -> AVPlayerView {
+        let view = AVPlayerView()
+        view.player = player
+        view.controlsStyle = .none
+        view.videoGravity = .resizeAspect
+        view.wantsLayer = true
+        view.layer?.backgroundColor = NSColor.black.cgColor
+        return view
     }
     
-    func updateNSView(_ nsView: PreviewPresentationLayer, context: Context) {
-        nsView.update(player: player, transform: transform)
+    func updateNSView(_ nsView: AVPlayerView, context: Context) {
+        if nsView.player !== player {
+            nsView.player = player
+        }
     }
 }
